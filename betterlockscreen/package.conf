UPSTREAM=https://github.com/betterlockscreen/betterlockscreen.git
VERSION=v4.4.0
# i3lock-color is not in Debian, so the DEP-8 testbed cannot resolve it from
# there. Hand it to autopkgtest alongside the built package.
DEP8_EXTRA_DEBS=i3lock-color
